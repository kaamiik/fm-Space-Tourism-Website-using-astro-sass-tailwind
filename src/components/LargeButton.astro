---

---

<a
  href="#"
  class="large-button | relative font-serif text-neutral-900 bg-neutral-100 text-[1.125rem] md:text-[2rem] uppercase px-[2.15em] rounded-full aspect-square inline-grid place-items-center z-[1]"
  >Explore</a
>

<style lang="scss">
  @use "../styles/abstracts/" as *;

  .large-button {
    &::after {
      content: "";
      position: absolute;
      background: $color-neutral-100;
      opacity: 0;
      width: 100%;
      height: 100%;
      border-radius: 100vh;
      z-index: -1;
      transition:
        opacity 500ms linear,
        transform 500ms ease-out;
    }

    &:hover::after,
    &:focus::after {
      opacity: 15%;
      transform: scale(1.5);
    }
  }
</style>
